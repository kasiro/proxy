[project]
name = "opencode-compat-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kasiro", email = "kon.konikov@mail.ru" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.100.0",
    "httpx>=0.25.0",
    "uvicorn>=0.20.0",
]

[project.scripts]
opencode-compat-proxy = "opencode_compat_proxy:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
